{
  // 🌍 Ρυθμίσεις περιοχών (regions)
  // Η σειρά δηλώνει προτεραιότητα:
  // 1️⃣ Φρανκφούρτη (fra1)
  // 2️⃣ Παρίσι (cdg1)
  // 3️⃣ Στοκχόλμη (arn1)
  "regions": ["fra1", "cdg1", "arn1"],

  // ⚙️ Ορισμός builds
  "builds": [
    {
      // Ό,τι υπάρχει στον φάκελο /api τρέχει ως Serverless Function
      "src": "api/**/*.js",
      "use": "@vercel/node"
    },
    {
      // Το index.html και όλα τα υπόλοιπα αρχεία σερβίρονται στατικά
      "src": "index.html",
      "use": "@vercel/static"
    }
  ],

  // 📦 Ο φάκελος εξόδου (όπου βρίσκεται το έτοιμο site)
  "outputDirectory": ".",

  // 🧠 Προαιρετικά: ρυθμίσεις για το cache, headers, κλπ.
  // (Παράδειγμα: κράτα cache για 1 ημέρα)
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=86400" }
      ]
    }
  ]
}
